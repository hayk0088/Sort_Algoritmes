def buble_sort(list):
    for x in range(len(list)-1):
        for i in range(len(list)-1-x):
            if list[i] > list[i+1]:
                list[i],list[i+1] = list[i+1],list[i]
    return list
    
